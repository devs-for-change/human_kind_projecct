# Human readable name of this type
name: Board Members

# Lowercase, underscored handle used to access this type
slug: board_members

# Explanatory text displayed in the back-office
description: Members of the board

# Slug of field used to identify entries by default, such as the title
label_field_name: name

# Valid values: manually, created_at, updated_at, or the slug of any field
order_by: name

# Valid values: asc (ascending) and desc (descending). Set to asc by default.
# order_direction: asc

# Specify a field slug to group entries by that field in the back-office.
# group_by: <your field>

# Activate public 'create' API (e.g for a contact form)
# public_submission_enabled: false

# Array of emails to be notified of new entries made with the public API
# public_submission_accounts: ['john@example.com']

# Control the display of the content type in the back-office.
display_settings:
  seo: true              # display the SEO tab for the content entries
  advanced: false         # display the Advanced tab for the content entries

# By default, the back-office displays the _label property (see label_field_name) of the content entry. This can be modified by writing your own Liquid template below:
# entry_template: '<a href="{{ link }}">{{ entry._label }}</a>' # The default template

# A list describing each field
fields:
- name: # The lowercase, underscored name of the field
    label: Name # Human readable name of the field
    type: string
    required: true
    hint: Name of Board Member
    localized: false

- bio: # The lowercase, underscored name of the field
    label: Bio # Human readable name of the field
    type: text
    required: true
    hint: Short biography of Board Member
    localized: false
    # text_formatting: html # html (uses rich text editor), markdown or text (uses plain text editor)

- website: # The lowercase, underscored name of the field
    label: Website # Human readable name of the field
    type: string
    required: false
    hint: Website of Board Member
    localized: false

- email: # The lowercase, underscored name of the field
    label: Email # Human readable name of the field
    type: string
    required: false
    hint: Email address of Board Member
    localized: false

- enabled: # The lowercase, underscored name of the field
    label: Currently Active? # Human readable name of the field
    type: boolean
    required: false
    hint: Currently a member of the board.
    localized: false

- photo: # The lowercase, underscored name of the field
    label: Photo # Human readable name of the field
    type: file
    required: false
    hint: Picutre of the Board member
    localized: false
